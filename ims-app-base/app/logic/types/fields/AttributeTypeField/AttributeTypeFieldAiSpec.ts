import type { AiSpecEntry } from '#logic/types/AiSpec';

export const attributeTypeFieldAiSpec: AiSpecEntry = {
  name: 'attributeType',
  title: '[[t:AttributeTypeField]]',
  aiSpec: {
    brief:
      'Attribute type selector field for choosing attribute types from the project registry.',
  },
};
