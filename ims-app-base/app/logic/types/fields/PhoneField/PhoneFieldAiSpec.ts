import type { AiSpecEntry } from '#logic/types/AiSpec';

export const phoneFieldAiSpec: AiSpecEntry = {
  name: 'phone',
  title: '[[t:PhoneField]]',
  aiSpec: {
    brief:
      'Phone number field with optimized input mode for telephone numbers.',
  },
};
