import type { AiSpecEntry } from '#logic/types/AiSpec';

export const fieldParamsFieldAiSpec: AiSpecEntry = {
  name: 'fieldParams',
  title: '[[t:FieldParamsField]]',
  aiSpec: {
    brief:
      'Internal field type for editing field controller parameters in the PropsBlock settings panel.',
  },
};
